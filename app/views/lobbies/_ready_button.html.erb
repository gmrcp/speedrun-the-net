  <%= link_to "Ready",
      ready_path,
      id: 'ready-button',
      attribute: 'disabled',
      class: "#{game_session.ready ? 'btn-enter' : 'btn-enter-outline' }
              #{game_session.sibling_game_sessions.count > 1 ? '' : 'd-none'}",
      data: {remote: true} %>
