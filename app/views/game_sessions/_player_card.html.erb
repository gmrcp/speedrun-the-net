<div data-play-banner-target="players"
     class="d-flex align-items-center p-1" <%= "id=current-player" if current_user == player %>>
  <h5 class="flex-fill text-truncate"><%= player.username %></h5>
  <p><%= game_session.clicks.count %></p>
  <i class="fas fa-hand-point-up ms-1"></i>
</div>
